import { ComponentFixture, TestBed } from '@angular/core/testing';
import { MttoWinInformeTecnico2Page } from './mtto-win-informe-tecnico2.page';

describe('MttoWinInformeTecnico2Page', () => {
  let component: MttoWinInformeTecnico2Page;
  let fixture: ComponentFixture<MttoWinInformeTecnico2Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(MttoWinInformeTecnico2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
